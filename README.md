Talking Terminal AI
Prerequisites

Install Node.js (https://nodejs.org
)

Setup

Clone or download this project.

In the project folder, create a .env file.

Add your API key:

GEMINI_API_KEY=your_api_key_here

Usage

Run a terminal command and pipe its output for AI explanation:

node index.js "your terminal error or output here"
